- hosts: all
  become: true
 # become_method: sudo
 # become_user: root
  tasks:
    - package: "name=rysnc state=present"
    - service: "name=rsync state=stopped"
    - service: "name=rsync state=started"
    #- service: "name=httpd state=started"
