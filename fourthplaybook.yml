--- 
- 
  become: true
  gather_facts: false
  hosts: all
  name: "To run command"
  tasks: 
    - 
      name: "run yum log command"
      register: his
      shell: "cat /etc/fstab"
    - 
      name: "run command"
      register: his
      shell: "df -h"
    - 
      debug: 
        var: his.stdout_lines
        verbosity: 2
